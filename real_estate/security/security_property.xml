<odoo>

    <!-- ================= GROUPS ================= -->

    <record id="group_agent" model="res.groups">
        <field name="name">Real Estate Agent</field>
    </record>

    <record id="group_property_manager" model="res.groups">
        <field name="name">Property Manager</field>
        <field name="implied_ids" eval="[(4, ref('group_agent'))]"/>
    </record>

    <record id="group_tenant" model="res.groups">
        <field name="name">Tenant</field>
    </record>
        <record id="maintainance_group" model="res.groups">
        <field name="name">Maintainance User</field>
    </record>
        <record id="maintainance_group_manager" model="res.groups">
        <field name="name">Maintainance Manager</field>
    </record>

    <record id="group_lease_user" model="res.groups">
        <field name="name">Lease Maker</field>
    </record>

    <record id="group_lease_manager" model="res.groups">
        <field name="name">Lease Manager</field>
    </record>

    <record id="group_tenant_manager" model="res.groups">
        <field name="name">Tenant Manager</field>
    </record>

    <record id="group_tenant_related" model="res.groups">
        <field name="name">Tenant Related</field>
    </record>


    <!-- ================= PROPERTY ================= -->

    <!-- Agent sees only his own properties -->
    <record id="property_agent_rule" model="ir.rule">
        <field name="name">Agent: Own Properties Only</field>
        <field name="model_id" ref="model_real_estate_property"/>
        <field name="domain_force">[('agent_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('group_agent'))]"/>
    </record>


    <!-- ================= TENANT ================= -->

    <!-- Tenant sees only tenants related to current user -->
    <record id="tenant_user_rule" model="ir.rule">
        <field name="name">Tenant: Own Tenants Only</field>
        <field name="model_id" ref="model_real_estate_tenant"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('group_tenant'))]"/>
    </record>


    <!-- Tenant Manager sees all tenants -->
    <record id="tenant_manager_rule" model="ir.rule">
        <field name="name">Tenant Manager: All Tenants</field>
        <field name="model_id" ref="model_real_estate_tenant"/>
        <field name="domain_force">[]</field>
        <field name="groups" eval="[(4, ref('group_tenant_manager'))]"/>
    </record>


    <!-- ================= PROPERTY MANAGER ================= -->

    <!-- Property Manager sees all properties -->
    <record id="property_manager_rule" model="ir.rule">
        <field name="name">Property Manager: All Properties</field>
        <field name="model_id" ref="model_real_estate_property"/>
        <field name="domain_force">[]</field>
        <field name="groups" eval="[(4, ref('group_property_manager'))]"/>
    </record>


    <!-- ================= LEASE ================= -->

    <!-- Lease Maker sees only his own leases -->
    <record id="lease_user_rule" model="ir.rule">
        <field name="name">Lease Maker: Own Leases Only</field>
        <field name="model_id" ref="model_real_estate_lease"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('group_lease_user'))]"/>
    </record>


    <!-- Lease Manager sees all leases -->
    <record id="lease_manager_rule" model="ir.rule">
        <field name="name">Lease Manager: All Leases</field>
        <field name="model_id" ref="model_real_estate_lease"/>
        <field name="domain_force">[]</field>
        <field name="groups" eval="[(4, ref('group_lease_manager'))]"/>
    </record>

    <!-- Maintainance Manager sees all requests -->
    <record id="maintainance_manager_rule" model="ir.rule">
        <field name="name">Maintainance Manager: All Requests</field>
        <field name="model_id" ref="model_maintenance_request"/>
        <field name="domain_force">[]</field>
        <field name="groups" eval="[(4, ref('maintainance_group_manager'))]"/>
    </record>
        <!-- Maintainance User -->
    <record id="maintainance_user_rule" model="ir.rule">
        <field name="name">Maintainance User</field>
        <field name="model_id" ref="model_maintenance_request"/>
        <field name="domain_force">[('assigned_to', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('maintainance_group'))]"/>
    </record>
</odoo>
